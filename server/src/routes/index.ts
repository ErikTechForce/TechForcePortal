import { Router } from 'express';

const router = Router();

// Placeholder for future routes
// Example: router.use('/orders', orderRoutes);
// Example: router.use('/clients', clientRoutes);

export default router;
